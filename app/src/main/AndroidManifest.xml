<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.chu.memo">

    <!-- 쓰기 권한은 암시적으로 읽기를 허용하므로 외부 저장소에 쓰려면 다음과 같이 하나의 권한만 요청하면 됩니다. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 애플리케이션의 필수 기능이 사진 촬영이라면 required="true", 사용하기는 하지만 꼭 핑료한 것이 아니라면 "false" 설정-->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET"/>


    <!-- Android 10(API 29) 이상을 타겟팅하는 경우 앱의 manifest 파일에서 requestLegacyExternalStorage의 값을 true로 설정합니다. -->
    <!-- 기본은 false 이며, Android 9이하를 타케팅하는 앱이 범위 지정 저장소를 사용할 때 어떻게 동작하는지 테스트할 수 있다. -->
    <!-- android:requestLegacyExternalStorage="true"-->
    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        tools:ignore="GoogleAppIndexingWarning">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        <activity android:name=".view.MemoActivity"-->
<!--            android:screenOrientation="portrait"/>-->

        <activity android:name=".view.activity.read.ReadActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".view.activity.AddActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>